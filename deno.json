{
  "tasks": {
    "dev": "deno serve --host 127.0.0.1 --watch -R main.ts",
    "test": "deno fmt && deno lint && deno test -R"
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@1",
    "@std/http": "jsr:@std/http@1"
  }
}
